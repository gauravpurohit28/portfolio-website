/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com'], 
        domains: ['i.giphy.com'], 
        
    },
};

export default nextConfig;
